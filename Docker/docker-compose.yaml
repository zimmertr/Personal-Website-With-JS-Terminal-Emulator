version: '3'
services:
    weatherpy:
        image: zimmertr/weatherpy:latest
        restart: always
        hostname: "weatherpy"
        volumes:
            - /mnt/SaturnPool/Apps/Weatherpy/weatherrc:/root/.weatherrc
            - /mnt/SaturnPool/Apps/Weatherpy/weather.txt:/root/weather.txt
    
    httpd:
        image: zimmertr/personal-website:latest
        depends_on: 
            - weatherpy
        restart: always
        hostname: "personal_website"
        ports:
            - "80:80"
        volumes:
            - /mnt/SaturnPool/Apps/Weatherpy/weather.txt:/var/www/cgi-bin/weather.txt
            - /mnt/SaturnPool/Apps/Apache/:/var/www/html/Files
