version: '3'
services:
    weatherpy:
        image: zimmertr/weatherpy:latest
        restart: always
        hostname: "weatherpy"
        volumes:
            - /Weatherpy/weatherrc:/root/.weatherrc
            - /Weatherpy/weather.txt:/root/weather.txt
    
    httpd:
        image: zimmertr/personal-website:latest
        depends_on: 
            - weatherpy
        restart: always
        hostname: "personal_website"
        ports:
            - "80:80"
        volumes:
            - /Weatherpy/weather.txt:/var/www/cgi-bin/weather.txt
            - /Files/:/var/www/html/Files
